source "http://rubygems.org"

# Specify your gem's dependencies in turnip.gemspec
gemspec

# If gherkin were pure ruby we could do this:
# gem 'gherkin', :git => 'https://github.com/leshill/gherkin.git', :branch => 'step_modules'
# Until, it is released (possibly as leshill-gherkin), we need to clone and
# make it available locally
gem 'gherkin', :path => '~/src/gherkin'
